{
  "name": "sakayna-functions",
  "private": true,
  "main": "src/index.js",
  "engines": { "node": "22" },
  "scripts": {
    "lint": "node --check src/index.js && node --check src/auth/guards.js && node --check src/users/userOperations.js && node --check src/assignments/assignmentOperations.js && node --check src/missions/missionOperations.js && node --check src/requests/requestOperations.js && node --check src/vehicles/vehicleOperations.js",
    "test": "node --test test/constants.test.js",
    "serve": "firebase emulators:start --only auth,firestore,functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "14.2.0",
    "firebase-functions": "7.3.0"
  },
  "devDependencies": {
    "firebase-tools": "14.27.0"
  }
}